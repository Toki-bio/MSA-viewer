# CLAUDE DESKTOP — DO NOT MODIFY THIS PROJECT

This is a working production web application. Claude Desktop must NOT:

- Edit, overwrite, or truncate script.js, styles.css, index.html, or server.js
- Modify ssh-servers.json (contains live server credentials/config)
- Create port forwards or SSH tunnels on port 3000 (this is the app's HTTP port)
- Install npm packages or modify package.json without explicit user instruction
- Run `node server.js` or start/stop the server without explicit user instruction

If asked to fix a bug or error in this project, READ the relevant file section first,
make a minimal targeted edit, and do NOT replace entire files.

The server is started manually by the user. Do not attempt to restart it.
